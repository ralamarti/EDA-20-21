package listas;

public class ListaCircular {
	
	/*
	 * Operaciones del TAD Lista Circular
	 * X isEmpty(): Determina si la lista es vacía
	 * insertAfter(n,x): Inserta el elemento x detrás del objeto n
	 * find(x): Devuelve el objeto con el elemento x
	 * remove(x): Elimina el elemento x
	 * previous(x): Devuelve el nodo anterior al elemento x 
	 * X clear(): Vacía la lista
	 * X size(): Devuelve el tamaño de la lista
	 * */

}
